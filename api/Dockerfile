FROM openjdk:17-jdk-slim

# Делаем папку внутри контейнера для нашего приложения
WORKDIR /app

# Копируем JAR-файл в контейнер.  Убедитесь, что имя JAR файла соответствует вашему.
COPY manager/target/*.jar app.jar

# Указываем, что контейнер слушает порт 8080 (или другой, который использует ваше приложение)
EXPOSE 8081

# Команда для запуска приложения
ENTRYPOINT ["java", "-jar", "app.jar"]